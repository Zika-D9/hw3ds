# Data Science Final HW - Unsupervised Learning & Anomaly Detection

## Context & Rules
- **Course:** Introduction to Data Science.
- **Dataset:** Credit Card Customer Data.
- **Assignment Document:** You MUST strictly reference and follow the exact requirements outlined in the `hw3 (1).pdf` file located in this directory. If you are unsure about any requirement, parse this PDF.
- **Output Format:** All work must be done in a single reproducible Jupyter Notebook. As per the rubric, all theoretical explanations, critical analysis, and discussions must be written inside text boxes (Markdown cells) within the notebook itself.

## Coding & Structure Standards
- Use modular, well-commented code with meaningful variable names.
- Do not use generic names like `df`, `x`, `data`.
- Create clean visualizations using `seaborn`/`matplotlib` with proper labels, titles, and legends.

## Git & GitHub Automation
- Before committing, always run `git status` to verify changes.
- Do NOT commit data files (ensure `.csv` or `archive/` directories are in `.gitignore`).
- Write clear, conventional commit messages.
- After a successful commit, automatically run `git push` to upload the changes to the remote GitHub repository. You do not need to ask for explicit permission to push if the commit was successful.